version: '3.4'

services:
  i3lab-api:
    image: ${DOCKER_REGISTRY-}i3labapi
    container_name: i3lab.api 
    build:
      context: .
      dockerfile: src/API/I3Lab.API/Dockerfile
    ports:
     - 5000:5000
     - 5001:5001


  i3lab-blob-storage:
     image: mcr.microsoft.com/azure-storage/azurite:lastest
     container_name: i3lab.blob-storage
     volumes: 
       - ./.containers/blob_storage/data:/data
     ports: 
       - "10000:10000" 
     command: "azurite-blob --blobHost 0.0.0.0 -l /data"












       # i3lab.database:
  #   image: postgres:latest
  #   container_name: i3lab.database
  #   environment:
  #     - POSTGRES_DB=i3labContext
  #     - POSTGRES_USER=postgres
  #     - POSTGRES_PASSWORD=postgres
  #   volumes:
  #     - ./.containers/i3labContext-db:/var/lib/postgresql/data
  #   ports:
  #     - 5432:5432
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U postgres"]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5